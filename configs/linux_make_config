# Extra includes
INCLUDES =

# Directories to not have in include path
INCLUDE_FILTER = 

# Directories/files to exclude from source
SRC_FILTER = %discord_register_win.cpp %connection_win.cpp %dllmain.cpp

# Libs to link and paths to link
LINKER_SEARCH_DIRS =
PRE_LINK_LIBRARIES =
POST_LINK_LIBRARIES =

# Extra defines
DEFINES = 

# Extra compiler flags
COMPILER_FLAGS =
LINKER_FLAGS =

# Name of your project's binary
BINARY_NAME = e